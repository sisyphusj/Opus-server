<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.opus.member.mapper.MemberMapper">

    <insert id="saveMember">
        insert into member (id, pw, nick, email) values (#{id}, #{pw}, #{nick}, #{email})
    </insert>

    <select id="checkDuplicateId" resultType="int">
        select count(*) from member where id=#{id}
    </select>

    <select id="checkDuplicateNick" resultType="int">
        select count(*) from member where nick=#{nick}
    </select>

    <select id="checkDuplicateEmail" resultType="int">
        select count(*) from member where email=#{email}
    </select>

    <select id="login" resultType="int">
        select mId from member where id=#{id} and pw=#{pw}
    </select>

    <select id="findById" resultType="com.opus.member.domain.Member">
        select * from member where mId=#{mId}
    </select>

    <update id="updateMember">
        update member set id=#{id}, pw=#{pw}, nick=#{nick}, email=#{email} where mId=#{mId}
    </update>

    <delete id="deleteMember">
        delete from member where mId=#{mId}
    </delete>

</mapper>